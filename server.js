// Standalone proxy entry point (npm start, launchers). The code lives in src/proxy/;
// this file stays at the repository root so existing launchers and shortcuts keep working.
import './src/proxy/server.js';
