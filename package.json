{
  "name": "sillytavern-claude-subscription",
  "version": "2.2.0",
  "description": "SillyTavern Server Plugin — OpenAI-compatible proxy backed by the Claude Agent SDK so chat bills against your Anthropic Pro/Max subscription. Fable 5.1 / Opus 5 / 1M context / reasoning effort / thinking display / quota meter.",
  "main": "plugin.js",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "license": "AGPL-3.0-or-later",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.141",
    "express": "^4.21.0"
  }
}
